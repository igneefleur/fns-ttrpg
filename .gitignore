# Cette branche ne porte que le hub (hub/index.html) : pas de build, pas de
# dépendance, donc presque rien à ignorer. Les règles d'exclusion des deux jeux
# vivent sur leurs branches, où elles ont un sens.
__pycache__/
*.pyc
